{
  "name": "widgets",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:widgets && npm run build:hammerspoon && npm run build:borders && npm run build:bartender && npm run build:warp",
    "build:widgets": "node scripts/build-widgets.mjs",
    "build:hammerspoon": "node scripts/build-hammerspoon-theme.mjs",
    "build:borders": "node scripts/build-borders-config.mjs",
    "build:bartender": "node scripts/build-bartender-theme.mjs",
    "build:thaw": "node scripts/build-thaw-theme.mjs",
    "build:warp": "node scripts/build-warp-theme.mjs",
    "theme": "node scripts/switch-theme.mjs",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/react": "^19.2.14",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.2"
  }
}
